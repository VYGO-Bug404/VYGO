# Requiere Python >= 3.10 (dataclasses con slots=True en vygo/schema.py).

# Núcleo numérico y entorno RL
numpy>=1.26
gymnasium>=0.29
numba>=0.60  # JIT del DP de held_karp: en Python puro no cabe en el presupuesto de tiempo

# Entrenamiento (MaskablePPO)
torch>=2.2
stable-baselines3>=2.3
sb3-contrib>=2.3

# Configuración y reportes
PyYAML>=6.0
pandas>=2.2

# L2 — red vial real de Monterrey (sólo demo/evaluación, nunca entrenamiento)
osmnx>=1.9
networkx>=3.2

# Pruebas
pytest>=8.0
